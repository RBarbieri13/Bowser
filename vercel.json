{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "dist/client",
  "functions": {
    "api/**/*.mjs": {
      "includeFiles": "data/*.{sqlite,json}",
      "maxDuration": 60
    }
  }
}
