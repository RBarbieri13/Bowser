# Compact Bowser controls — September 17, 2026

One consistent PageControls header per main page. Page title and concise active-context summary share a compact row with essential actions and a Filters & settings toggle. Secondary filters, source notes, configuration, week pickers, and trend setup belong inside this single collapsed-by-default region; preserve all unique controls, saved state, dates, provenance, player links and table semantics. No new table-settings replacement. Tables/first data should begin within 200px of page top at desktop, except meaningful data error/loading states. Target <=140px for Market/Players/Waivers, <=220px for Opportunity and Team including their table section labels. Hidden filters retain state and show active context in header. Opening via keyboard works; closing never resets values. Coarse-pointer controls at least44px; compact desktop controls32px.

Hide League Hub and Fantasy Intelligence from navigation, preserve their code/private data and reroute old page links to Player Database. Team matchup labels occur once, position-specific metric headers remain as needed; shorten rows without clipping. Retain anchored historical trends.

Thursday September17 DFS must use a real official slate with explicit Classic versus Showdown/Captain semantics, date/game/source provenance, separate role prices if required. No substitute all-week slate relabeled as Thursday-only. Historical captures stay immutable. Fix dynamic-current-data tests to verify source-based values and retain exact immutable archived snapshot tests.

Verification: baseline/after desktop and narrow captures, per-page filter inventory retained, independent keyboard/dense-user reviews, no new page overflow/errors, npm run check, hosted/public API readbacks, and main Vercel URL verification. Max3 correction passes per repeated failure;180-minute review point. Preserve dirty main checkout. Existing production code f1259e6; working branch starts evidence commit e9791be.
