### Result
{"url":"https://fantasy-football-stats-preview.vercel.app/#/market-pulse","marketRows":100,"marketSettings":0,"marketNumericFilters":0,"opportunityCards":30,"opportunitySettings":0,"teamSettings":0,"teamNewSearch":0,"teamTrendControl":true,"waiversNativeTableMenu":true,"waiverSharedApply":0}
### Ran Playwright code
```js
await (async page => { const out={url:page.url()}; await page.getByRole("button",{name:"Refresh data",exact:true}).click(); await page.waitForSelector(".mp-player"); out.marketRows=await page.locator(".mp-player").count(); out.marketSettings=await page.getByRole("button",{name:"Table settings",exact:true}).count(); out.marketNumericFilters=await page.getByText(/Numeric filters/).count(); await page.getByRole("link",{name:"Opportunity Tracker",exact:true}).click(); await page.getByRole("article",{name:"Cam Skattebo opportunity",exact:true}).waitFor(); out.opportunityCards=await page.getByRole("article").count(); out.opportunitySettings=await page.getByRole("button",{name:"Table settings",exact:true}).count(); await page.getByRole("link",{name:"Team Box Scores",exact:true}).click(); await page.waitForSelector(".boxscore-table tbody tr"); out.teamSettings=await page.getByRole("button",{name:"Table settings",exact:true}).count(); out.teamNewSearch=await page.getByLabel("Player search",{exact:true}).count(); out.teamTrendControl=await page.getByRole("button",{name:"Add trend column",exact:true}).isVisible(); await page.getByRole("link",{name:"Waivers",exact:true}).click(); await page.getByRole("button",{name:"Table",exact:true}).click(); await page.getByRole("combobox",{name:"Waiver row density",exact:true}).waitFor(); out.waiversNativeTableMenu=true; out.waiverSharedApply=await page.getByRole("button",{name:"Apply settings",exact:true}).count(); if(out.marketSettings||out.marketNumericFilters||out.opportunitySettings||out.teamSettings||out.teamNewSearch||out.waiverSharedApply)throw Error("New controls remain"); await page.getByRole("link",{name:"Market Pulse",exact:true}).click(); return out; })(page);
```
