# Weekly DFS refresh and release

Authorized by Robert September 16, 2026: weekly sourced DraftKings salaries/projections, historical storage, and deployment to the main Bowser app. Run Thursday at 08:00 America/Chicago in the attached Codex task.

1. Read this task's latest successful release record and Vercel deployment metadata. Start from that exact application commit in a new isolated checkout under the Fantasy Football workspace. Never overwrite unrelated changes or revert a newer deployed app to an older checkout. Preserve the current archive, all prior captures, and the 2025/2026 statistical warehouses.
2. Run `python3 scripts/import_dfs_weekly.py`. It discovers the upcoming season/week and unlocked official Classic slates, validates dated schedule, exact official salaries, primary projections, and supplemental source matches. It stages the JSON and SQLite outputs before publication. Any error or `publishable:false` stops deployment. `changed:false` is terminal for this run: do not commit, build, redeploy, or report unchanged state.
3. For a changed verified capture, run `python3 scripts/import_dfs_weekly.py --verify` and `npm run check`. Preserve missing source values as null. Never weaken checks or fabricate values to make a release pass. Inspect the resulting salary/projection counts, date, source provenance and historical capture readback.
4. Save a local release commit. Use the existing GitHub workflow when credentials are working; if that route is unavailable, the authorized direct Vercel release may proceed from the verified local commit. Do not create another Vercel project or change environment secrets.
5. Deploy a candidate with `vercel deploy --prod --skip-domain --yes`. Run `python3 scripts/verify_trend_release.py --vercel --url <candidate> --output <evidence.json>`. Only after PASS promote that exact candidate with `vercel promote <candidate> --yes`.
6. Verify `https://fantasy-football-stats-preview.vercel.app` using the same readback script without `--vercel`. Confirm its current dated DFS fields match the captured local data and its historical records remain available. Save deployment URL/ID, code commit, source counts and verifier results in the release record for the next run.
7. On any failed importer, test, deployment or public check, stop; retain the prior verified production release and report the concrete failure or required user action. Notify only on a changed successful release, failure, or required action. Do not notify for an unchanged/non-actionable state.

The scheduler refreshes DFS data. Existing weekly actual-stat imports remain separate. Historical DFS prices/projections exist only for verified captured weeks; 2025 actual statistics remain accessible, but no unsupported historical pregame prices are fabricated. Market Pulse history is provider observations stored in each browser, not NFL game weeks.
